import { useEffect,useState } from 'react'; import { api } from '../lib/api'; import StatCard from '../components/StatCard'; import { PieChart,Pie,Cell,ResponsiveContainer,Tooltip,LineChart,Line,XAxis,YAxis } from 'recharts';
export default function Dashboard(){const [data,setData]=useState<any>();useEffect(()=>{api.get('/dashboard/summary').then(r=>setData(r.data.data))},[]); if(!data)return <p>Loading dashboard...</p>; const trend=[{m:'Jan',v:400},{m:'Feb',v:700},{m:'Mar',v:650},{m:'Apr',v:900},{m:'May',v:data.totalIncome}]; return <div><h1 className="text-3xl font-black">Financial Dashboard</h1><p className="mt-2 text-slate-400">Real-time income, expenses, payroll, invoices and AI recommendations.</p><section className="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-4"><StatCard label="Total Income" value={`₦${data.totalIncome.toLocaleString()}`} sub="Paid invoices"/><StatCard label="Total Expenses" value={`₦${data.totalExpenses.toLocaleString()}`} sub="Approved + pending"/><StatCard label="Net Profit/Loss" value={`₦${data.netProfit.toLocaleString()}`} sub={data.netProfit>=0?'Positive cash flow':'Review spending'}/><StatCard label="Pending Invoices" value={String(data.pendingInvoices)} sub="Require follow-up"/></section><section className="mt-6 grid gap-4 xl:grid-cols-2"><div className="card p-6"><h2 className="font-bold">Cash Flow</h2><ResponsiveContainer width="100%" height={260}><LineChart data={trend}><XAxis dataKey="m"/><YAxis/><Tooltip/><Line type="monotone" dataKey="v" strokeWidth={3}/></LineChart></ResponsiveContainer></div><div className="card p-6"><h2 className="font-bold">Expense Categories</h2><ResponsiveContainer width="100%" height={260}><PieChart><Pie data={data.expensesByCategory} dataKey="value" nameKey="_id" outerRadius={100}>{data.expensesByCategory.map((_:any,i:number)=><Cell key={i}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div></section><div className="card mt-6 p-6"><h2 className="font-bold">AI Recommendation</h2><p className="mt-3 text-slate-300">{data.aiRecommendation}</p></div></div>}
