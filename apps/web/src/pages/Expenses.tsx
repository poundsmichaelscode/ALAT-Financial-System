import { useEffect,useState } from 'react'; import { api } from '../lib/api';
export default function Expenses(){const [items,setItems]=useState<any[]>([]);const [form,setForm]=useState({title:'',amount:'',category:'Miscellaneous'});const load=()=>api.get('/expenses').then(r=>setItems(r.data.data.items));useEffect(load,[]);async function add(e:any){e.preventDefault();await api.post('/expenses',{...form,amount:Number(form.amount)});setForm({title:'',amount:'',category:'Miscellaneous'});load()}return <div><h1 className="text-3xl font-black">Expenses</h1><form onSubmit={add} className="card mt-6 grid gap-3 p-5 md:grid-cols-4"><input className="input" placeholder="Expense title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><input className="input" placeholder="Amount" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})}/><select className="input" value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{['Transportation','Utilities','Salary','Marketing','Food','Maintenance','Office supplies','Tax','Miscellaneous'].map(c=><option className="text-black" key={c}>{c}</option>)}</select><button className="btn">Add Expense</button></form><div className="card mt-6 overflow-hidden"><table className="w-full text-left"><thead className="bg-white/10"><tr><th className="p-4">Title</th><th>Category</th><th>Amount</th><th>Status</th></tr></thead><tbody>{items.map(i=><tr key={i._id} className="border-t border-white/10"><td className="p-4">{i.title}</td><td>{i.category}</td><td>₦{i.amount?.toLocaleString()}</td><td>{i.approvalStatus}</td></tr>)}</tbody></table></div></div>}
