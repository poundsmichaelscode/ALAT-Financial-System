import { useEffect,useState } from 'react'; import { api } from '../lib/api';
export default function Invoices(){const [items,setItems]=useState<any[]>([]);const load=()=>api.get('/invoices').then(r=>setItems(r.data.data.items));useEffect(load,[]);return <div><h1 className="text-3xl font-black">Invoices</h1><p className="mt-2 text-slate-400">Create, track, download and email invoices.</p><div className="card mt-6 overflow-hidden"><table className="w-full text-left"><thead className="bg-white/10"><tr><th className="p-4">Invoice</th><th>Client</th><th>Total</th><th>Status</th></tr></thead><tbody>{items.map(i=><tr key={i._id} className="border-t border-white/10"><td className="p-4">{i.invoiceNumber}</td><td>{i.clientName}</td><td>₦{i.total?.toLocaleString()}</td><td>{i.status}</td></tr>)}</tbody></table></div></div>}
