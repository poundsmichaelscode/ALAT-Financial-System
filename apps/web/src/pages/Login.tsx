import { useState } from 'react'; import { useDispatch } from 'react-redux'; import { useNavigate } from 'react-router-dom'; import { login } from '../store/authSlice';
export default function Login(){const [email,setEmail]=useState('admin@alatfinance.com');const [password,setPassword]=useState('Password123!');const [err,setErr]=useState('');const dispatch=useDispatch<any>();const nav=useNavigate(); async function submit(e:any){e.preventDefault();setErr('');try{await dispatch(login({email,password})).unwrap();nav('/')}catch{setErr('Authentication failed. Check API server and credentials.')}} return <div className="grid min-h-screen place-items-center bg-slate-950 p-6"><form onSubmit={submit} className="card w-full max-w-md p-8"><h1 className="text-3xl font-black">Welcome back</h1><p className="mt-2 text-slate-400">Sign in to ALAT Financial System.</p><div className="mt-8 space-y-4"><input className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/>{err&&<p className="text-red-300">{err}</p>}<button className="btn w-full">Login</button></div></form></div>}
