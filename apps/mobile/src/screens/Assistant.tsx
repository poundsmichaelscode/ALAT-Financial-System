import { useState } from 'react';
import { View, Text, TextInput, Pressable, StyleSheet, ScrollView } from 'react-native';
import { api } from '../lib/api';
export default function Assistant(){const [prompt,setPrompt]=useState('Predict next month expenses');const [answer,setAnswer]=useState('');async function ask(){const r=await api.post('/ai/ask',{prompt});const a=r.data.data.answer;setAnswer(typeof a==='string'?a:a.summary)}return <ScrollView style={s.container}><Text style={s.title}>AI Finance Assistant</Text><TextInput multiline value={prompt} onChangeText={setPrompt} style={s.input}/><Pressable onPress={ask} style={s.button}><Text style={s.btn}>Ask AI</Text></Pressable>{answer ? <Text style={s.answer}>{answer}</Text> : null}</ScrollView>}
const s=StyleSheet.create({container:{flex:1,backgroundColor:'#170C79',padding:20},title:{color:'#fff',fontSize:26,fontWeight:'800'},input:{marginTop:20,minHeight:120,borderRadius:18,backgroundColor:'rgba(255,255,255,0.10)',color:'#fff',padding:16},button:{marginTop:16,backgroundColor:'#56B6C6',borderRadius:18,padding:16},btn:{textAlign:'center',fontWeight:'800',color:'#170C79'},answer:{marginTop:18,color:'#EFE3CA',backgroundColor:'rgba(255,255,255,0.10)',padding:16,borderRadius:18}});
